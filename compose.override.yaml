services:
  configurator:
    environment:
      PROJECT_CLONE_URL: https://github.com/ajeetraina/labspace-developer-productivity

  mcp-gateway:
    image: docker/mcp-gateway:v0.40.0
    container_name: labspace-mcp-gateway
    command:
      - --port=8080
      - --transport=sse
      - --servers=fetch,duckduckgo,context7
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
